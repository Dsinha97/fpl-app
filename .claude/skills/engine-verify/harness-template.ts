// Scratch harness -- verify a real module against live data.
// Copy this to the session scratchpad before editing. DO NOT COMMIT. Delete after use.

import { /* functionUnderTest */ } from "../lib/FILL_ME_IN";

async function main() {
  // Fill in real inputs -- pull actual player/squad/gameweek data,
  // don't invent fixture data that could hide the bug.
  const input = {};

  const result = /* functionUnderTest */(input);
  console.log(JSON.stringify(result, null, 2));
}

main();
